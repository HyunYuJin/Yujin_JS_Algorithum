// 사용자가 입력한 양의 정수의 각 자리수의 합 구하기

const input = prompt("양의 정수를 입력하세요.").split("");

let sum = input.reduce((a, b) => {
  return parseInt(a, 10) + parseInt(b, 10);
});

console.log(sum);


// ------------------------------------------------------

// 수학적인 방법으로 풀어보기!💁🏻‍♂️
// "1234"를 10으로 나눈 나머지의 값끼리 더해나간다.

let num = "1234";
let sum = 0;

while(num) {
    sum += num % 10; // 4 + 3 + 2 + 1
    num = num / 10; // 123 -> 12 -> 1 -> 0
}

console.log(sum);
